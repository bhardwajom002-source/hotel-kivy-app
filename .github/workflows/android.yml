- name: Install Android SDK (FINAL FIX)
  run: |
    mkdir -p /home/runner/android-sdk
    echo "ANDROID_HOME=/home/runner/android-sdk" >> $GITHUB_ENV
    echo "ANDROID_SDK_ROOT=/home/runner/android-sdk" >> $GITHUB_ENV

    cd /home/runner
    wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
    unzip commandlinetools-linux-11076708_latest.zip

    mkdir -p android-sdk/cmdline-tools/latest
    mv cmdline-tools/* android-sdk/cmdline-tools/latest/

    export PATH=/home/runner/android-sdk/cmdline-tools/latest/bin:$PATH

    yes | sdkmanager --licenses

    sdkmanager \
      "platform-tools" \
      "platforms;android-33" \
      "build-tools;30.0.3" \
      "ndk;25.2.9519653"

    # 🔥🔥 FINAL BUILD OZER FIX 🔥🔥
    mkdir -p /home/runner/android-sdk/tools/bin
    ln -s /home/runner/android-sdk/cmdline-tools/latest/bin/sdkmanager \
          /home/runner/android-sdk/tools/bin/sdkmanager

    echo "/home/runner/android-sdk/build-tools/30.0.3" >> $GITHUB_PATH
